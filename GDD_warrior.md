# 📜 [프로젝트: 타임라인] 캐릭터 기획서: 전사 (Warrior)

> 플레이어 캐릭터 1번. 설계형 탱커.
> 묵직한 공방 교환과 정교한 방어도 계산으로 타이밍을 재고,
> 남는 행동력을 축적하여 결정적인 한 방을 날린다.

---

## 1. 기본 스탯

| 항목 | 수치 |
|------|------|
| 최대 HP | 50 |
| 기본 AP | 3 (매 플레이어 턴 시작 시 초기화) |
| 액티브 슬롯 | 2칸 |

---

## 2. 고유 능력: 투기 발산 (Innate Ability) ✅ 구현 완료

카드를 사용하지 못해 버려지는 AP를 자원으로 치환하여, 패가 꼬인 상황을 타개하는 안전망이자 궁극기 시스템.

### 패시브 — 투기 축적
- 플레이어 턴 종료 시, 사용하지 않고 남은 AP 1당 **투기 스택 1** 획득
- 최대 10스택

### 액티브 — 투기 발산 (0 AP)
- 투기 스택이 **10**에 도달하면 활성화
- **효과:** 보스에게 즉시 **10 피해** + 플레이어 **방어도 10** 획득
- 발동 후 스택 0으로 초기화

### 구현
- `scripts/characters/warrior/rage_system.gd` — `WarriorRageSystem` (RefCounted, `GameContext` 주입)
- UI: `BuffBar` 내부 `RageLabel` + 10칸 `RageOrbs` + `RageButton` (ResourceBar 우측)
- 트리거: `main_scene._finish_turn()`에서 `rage_system.add(ap_manager.current)` 호출

**설계 의도:** 카드 직접 사용(1 AP → 2~3 가치)보다 낮은 효율(1 AP → 평균 2 가치 분산)로 설정.
카드를 쓰는 것이 항상 최선이되, 버려지는 AP가 없도록 보완한다.

---

## 3. 스타터 덱 구성 (총 10장)

전투 시작 시 파이프에 아래 순서대로 배치됩니다.
턴 시작 시 파이프 앞 5장을 손패로 드로우합니다.

### 3.1 1번 손패 (파이프 1~5번, 첫 턴에 드로우)

| 카드명 | 타입 | AP | 효과 |
|--------|------|----|------|
| 골드 카드 | 경제 | 0 | 골드 +1 |
| 골드 카드 | 경제 | 0 | 골드 +1 |
| 골드 카드 | 경제 | 0 | 골드 +1 |
| 베기 | 공격 | 1 | 보스 피해 2 |
| 막기 | 방어 | 1 | 방어도 +3 |

### 3.2 2번 손패 (파이프 6~10번, 두 번째 드로우)

| 카드명 | 타입 | AP | 효과 |
|--------|------|----|------|
| 골드 카드 | 경제 | 0 | 골드 +1 |
| 골드 카드 | 경제 | 0 | 골드 +1 |
| 베기 | 공격 | 1 | 보스 피해 2 |
| 막기 | 방어 | 1 | 방어도 +3 |
| 집중 | 조작 | 0 | 카드 1장 드로우 |

---

## 4. 고유 시작 모듈 ✅ 구현 완료

전투 시작 시 액티브 슬롯 1에 기본 장착된 패시브 아이템.

### [모듈] 반격 태세
- **AP:** 0 (전투 시작 시 장착 상태)
- **효과:** 보스의 턴이 끝날 때, 플레이어 방어도가 1 이상 남아있으면 보스에게 **2 피해**

### 구현
- `scripts/characters/warrior/counter_stance_ability.gd` — `ModuleAbility` 상속, `on_boss_turn_end` 오버라이드
- 리소스: `resources/cards/warrior/module_counter_stance.tres`
- 장착: `main_scene._equip_warrior_modules()`가 시작 시 `ActiveSlot1`에 배치 (`set_active(false)`로 드래그 잠금)
- 트리거: `_begin_boss_turn()`에서 보스 피해 처리 후 `active_cards` 순회하며 `on_boss_turn_end(ctx)` 호출

**설계 의도:** 정밀한 방어 계산에 대한 보상. 소량 피해라도 누적시켜 전투를 유리하게 이끈다.
방어도가 **라운드 종료 시점까지 유지**되도록 변경되면서 (2026-04-15) 반격 태세의 발동 기회가 크게 늘어났다.

---

## 5. 핵심 시스템 정의

### 골드 소멸
턴 종료 시 남은 골드는 전량 증발한다.
이번 턴에 골드 카드를 몇 장 뽑아 마켓의 어떤 카드를 살지 **타이밍**을 설계해야 한다.

### 방어도 — 라운드 유지
플레이어 방어도는 **라운드 종료 시점**까지 유지된다 (5턴 내내 누적).
이로써 막기 카드는 축적 가능한 자원이 되고, 반격 태세 모듈과 투기 발산의 방어도 보너스가 모두 온 라운드에 걸쳐 의미를 가진다.

### 타임라인 프로그래밍
턴 종료 시 사용/남은 카드를 원하는 순서대로 정렬하여 파이프 맨 뒤에 추가한다.
버린 순서 그대로 다시 드로우되므로 **지금 버리는 순서가 미래를 결정한다.**

---

## 6. 라운드 마켓 — 전사 카드 풀 ✅ 구현 완료

라운드 시작 시 **보스 페이즈별 가중치 추첨**으로 3장 진열. 각 카드별 고정 골드 가격. AP 3 또는 골드 3으로 리롤 가능.
구매한 카드는 즉시 파이프 맨 뒤에 추가된다.

### Tier 1 — 기본 풀
| 카드명 | 타입 | AP | 골드 가격 | 효과 |
|--------|------|----|-----------|------|
| 강타 | 공격 | 2 | 4 | 보스에게 5 피해 |
| 굳건한 방패 | 방어 | 1 | 3 | 방어도 +5 |
| 돌격 | 공격 | 1 | 4 | 보스에게 3 피해 + 방어도 +2 |
| 명상 | 조작 | 0 | 4 | 카드 2장 드로우 |

### Tier 2 — 페이즈 2+ 주력 풀
| 카드명 | 타입 | AP | 골드 가격 | 효과 |
|--------|------|----|-----------|------|
| 참격 | 공격 | 2 | 6 | 보스에게 8 피해 |
| 강철 의지 | 방어 | 1 | 5 | 방어도 +8 |
| 속공 | 공격 | 1 | 6 | 보스에게 4 피해 + 카드 1장 드로우 |
| 재정비 | 조작 | 0 | 5 | 카드 3장 드로우 |

### 페이즈 연동
- Phase 1: T1 100%
- Phase 2: T1 20 · T2 100
- Phase 3: T1 5 · T2 30 · T3 100 (T3 카드 미구현 — 현재는 T2가 주)
- 누적 방식이지만 하위 티어 확률이 급감

### 구현
- `scripts/ui/market_panel.gd` — `MarketPanel` (PanelContainer) + `current_phase` + `_weighted_pick`
- 리소스: `resources/cards/warrior/market/market_*.tres` (T1, 4종) + `tier2/market_*.tres` (T2, 4종)
- 풀 할당: `scenes/main/main_scene.tscn`의 `MarketPanel.card_pool` (T1 + T2 총 8장)
- 구매 핸들러: `main_scene._on_market_card_purchased` → `queue_cards`에 추가
- 페이즈 연동: `main_scene._on_phase_changed` → `market_panel.set_phase(new_phase)`

**설계 의도:** 골드 소멸 규칙과 짝을 이루는 자원 루프 폐쇄. "이번 턴 골드 몇 개로 어떤 카드를 살 것인가"를 결정해야 하므로 골드 카드 사용 타이밍이 의미를 갖는다. 구매 카드는 파이프 맨 뒤에 들어가므로 즉효성보다는 라운드 중반 이후 손패 강화에 가깝다. 페이즈가 진행될수록 T2 카드가 주력으로 등장해 **후반부 골드 누적의 의미**가 커진다.

---

## 7. 미구현 시스템 (개발 예정)

| 시스템 | 우선순위 | 내용 |
|--------|----------|------|
| Tier 3 마켓 카드 | 중 | Phase 3 주력 카드 풀 (현재 T3 가중치는 있으나 카드 없음) |
| 추가 모듈 카드 | 중 | 반격 태세 외 선택지 제공 |
| 분노 직접 조작 카드 | 낮음 | "분노의 함성" 등 투기 스택을 직접 다루는 마켓/스타터 카드 |

---

## 8. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-04-17 | PhaseLabel 위치 변경 — TurnInfoPanel → 보스 정보 영역(BossInfoRight) 상단으로 이동, font_size 20 |
| 2026-04-17 | 페이즈 진입 배너 `Array[String]` 타입 불일치 오류 수정 |
| 2026-04-16 | 전사 마켓 Tier 2 카드 4종(참격·강철 의지·속공·재정비) 추가, 보스 페이즈 연동 가중치 추첨 |
| 2026-04-15 | 라운드 마켓 + 전사 마켓 카드 풀 4종(강타·굳건한 방패·돌격·명상) 구현 |
| 2026-04-15 | 방어도 리셋 시점: 턴 종료 → 라운드 종료로 변경 (반격 태세 · 투기 발산 발동 기회 확대) |
| 2026-04-15 | 투기 발산 시스템 + UI 구현 완료 (`WarriorRageSystem`, `BuffBar` 재사용) |
| 2026-04-15 | 반격 태세 모듈 구현 완료 (`CounterStanceAbility`, `module_counter_stance.tres`) |
| 2026-04-15 | 초안 작성. 예비 슬롯·살기 시스템 제외, 타임라인 방식 FIFO로 확정 |
